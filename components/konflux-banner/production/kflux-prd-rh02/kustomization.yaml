apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: konflux-banner

resources:
  - ../../base

configMapGenerator:
  - name: konflux-banner-config
    behavior: merge
    envs:
      - banner-path.env

generatorOptions:
  disableNameSuffixHash: true
